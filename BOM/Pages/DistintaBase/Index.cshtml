@page
@model BOM.Pages.DistintaBase.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DistintaBase[0].IdVersion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DistintaBase[0].IdSon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DistintaBase[0].Amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DistintaBase) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdVersion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
